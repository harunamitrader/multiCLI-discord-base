interface:
  display_name: "Schedule Manager"
  short_description: "Manage cron schedule JSON jobs for CoDiCoDi"
  default_prompt: "Use $schedule-manager to create, edit, pause, resume, list, or delete CoDiCoDi schedule jobs in data/schedules."
