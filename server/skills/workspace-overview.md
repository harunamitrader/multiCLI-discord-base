# Workspace overview

- Respect the active workspace root and keep edits inside it unless explicitly requested.
- Prefer the shared PTY flow used by Chat, Terminal, Discord, and Schedule.
- Surface blockers plainly instead of masking them with fallback output.
